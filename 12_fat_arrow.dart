// fat arrow functions is only used for single expressions
// fat arrow functions is not much used because it operates with only single expression

// fat arrow function
int add(var a, var b) => a + b;

void main() {
  //
  print(add(40, 20));
}
